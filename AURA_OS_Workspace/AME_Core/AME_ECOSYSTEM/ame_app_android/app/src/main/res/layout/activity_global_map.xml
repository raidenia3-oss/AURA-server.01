<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0D1117"
    android:padding="16dp">

    <!-- Header -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🌍 AURA GLOBAL COMMAND MAP"
        android:textColor="#00FF41"
        android:textSize="18sp"
        android:textStyle="bold"
        android:gravity="center"
        android:paddingBottom="8dp" />

    <TextView
        android:id="@+id/tvMapStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Infraestructura del ecosistema AURA/AME"
        android:textColor="#8B949E"
        android:textSize="12sp"
        android:gravity="center"
        android:paddingBottom="12dp" />

    <!-- Mapa Canvas placeholder -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#161B22"
        android:layout_marginBottom="12dp">

        <WebView
            android:id="@+id/mapWebView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <TextView
            android:id="@+id/tvMapPlaceholder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="🗺️ Mapa se cargará\ncon coordenadas del backend"
            android:textColor="#58A6FF"
            android:textSize="14sp"
            android:gravity="center"
            android:visibility="visible" />
    </FrameLayout>

    <!-- Nodos del ecosistema -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📡 NODOS ACTIVOS"
        android:textColor="#58A6FF"
        android:textSize="14sp"
        android:textStyle="bold"
        android:paddingBottom="6dp" />

    <LinearLayout
        android:id="@+id/nodesContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" />

    <!-- Botón Actualizar -->
    <Button
        android:id="@+id/btnRefreshMap"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="8dp"
        android:text="🔄 ACTUALIZAR MAPA"
        android:textColor="#FFFFFF"
        android:backgroundTint="#238636"
        android:textAllCaps="false" />

</LinearLayout>
